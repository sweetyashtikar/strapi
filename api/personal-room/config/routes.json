{
  "routes": [
    {
      "method": "GET",
      "path": "/personal-rooms",
      "handler": "personal-room.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/personal-rooms/count",
      "handler": "personal-room.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/personal-rooms/:slug",
      "handler": "personal-room.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/personal-rooms",
      "handler": "personal-room.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/personal-rooms/:id",
      "handler": "personal-room.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/personal-rooms/:id",
      "handler": "personal-room.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
